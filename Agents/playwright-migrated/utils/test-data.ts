/**
 * Central place for test data used by migrated step definitions, matching
 * whatever the original Cucumber scenario's data source was (a
 * `Scenario Outline`'s `Examples` table, a `DataTable` step argument, a
 * Java constant/properties file) — see the "Test Data Strategy" section
 * of each migration/<slug>/plan.md.
 *
 * Keep this file organized by feature/page as it grows; don't let it
 * become one flat unstructured bag.
 */

export {};
